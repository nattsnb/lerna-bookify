{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/shared/prisma-client"
  ],
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "lerna": "^8.2.2",
    "prisma": "^6.7.0",
    "@prisma/client": "^6.6.0"
  },
  "scripts": {
    "build-prisma": "prisma generate --schema packages/backend/prisma/schema",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream"
  }
}
